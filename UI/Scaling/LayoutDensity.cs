using System;

namespace WWXMapEditor.UI.Scaling
{
    // Simple density tiers used by converters and optional density-based styling.
    public enum LayoutDensity
    {
        Compact = 0,
        Normal = 1,
        Spacious = 2
    }
}