<Window x:Class="WWXMapEditor.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WWXMapEditor.Views"
        xmlns:scaling="clr-namespace:WWXMapEditor.UI.Scaling"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" 
        Height="1080" 
        Width="1920"
        MinHeight="720"
        MinWidth="1280"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Background="{DynamicResource BackgroundBrush}"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True">
    <Grid>
        <!-- Keep the main menu layer and the content host as before -->
        <local:MainMenuView x:Name="MainMenu" Visibility="{Binding MainMenuVisibility}"/>
        <ContentControl Content="{Binding CurrentView}" 
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"/>
    </Grid>
</Window>