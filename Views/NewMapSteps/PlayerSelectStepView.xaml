<UserControl x:Class="WWXMapEditor.Views.NewMapSteps.PlayerSelectStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WWXMapEditor.Views.NewMapSteps"
             xmlns:scaling="clr-namespace:WWXMapEditor.UI.Scaling"
             xmlns:conv="clr-namespace:WWXMapEditor.Converters"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1200">

    <UserControl.Resources>
        <!-- Converters -->
        <conv:MultiplyConverter x:Key="MultiplyConverter"/>
        <conv:IntComparisonConverter x:Key="IntComparisonConverter"/>

        <!-- Section header styles (matching Settings/Configuration) -->
        <Style x:Key="SectionKicker" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Opacity" Value="0.95"/>
        </Style>
        <Style x:Key="SectionSub" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundSecondaryBrush}"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="Opacity" Value="0.9"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <!-- Card container style -->
        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource Surface3Brush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#AA000000" Direction="270" ShadowDepth="2" Opacity="0.18" BlurRadius="8"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Country list (Random first by default) -->
        <x:Array x:Key="CountryOptions" Type="{x:Type sys:String}">
            <sys:String>Random</sys:String>
            <sys:String>Redonia</sys:String>
            <sys:String>Sevaria</sys:String>
            <sys:String>Auria</sys:String>
            <sys:String>Maritain</sys:String>
            <sys:String>Norvos</sys:String>
            <sys:String>Valtro</sys:String>
        </x:Array>

        <!-- ItemTemplate showing a flag color swatch + country name -->
        <DataTemplate x:Key="CountryItemTemplate">
            <StackPanel Orientation="Horizontal">
                <Border x:Name="FlagSwatch"
                        Width="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale, Converter={StaticResource MultiplyConverter}, ConverterParameter=18}"
                        Height="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale, Converter={StaticResource MultiplyConverter}, ConverterParameter=18}"
                        CornerRadius="3"
                        BorderThickness="1"
                        BorderBrush="{DynamicResource BorderBrush}"
                        Background="Transparent"
                        Margin="0,0,8,0"/>
                <TextBlock Text="{Binding}"
                           FontFamily="Consolas"
                           Foreground="{DynamicResource ForegroundBrush}"/>
            </StackPanel>
            <DataTemplate.Triggers>
                <!-- Map each country to its flag color -->
                <DataTrigger Binding="{Binding}" Value="Redonia">
                    <Setter TargetName="FlagSwatch" Property="Background" Value="#2ECC71"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding}" Value="Sevaria">
                    <Setter TargetName="FlagSwatch" Property="Background" Value="#9E9E9E"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding}" Value="Auria">
                    <Setter TargetName="FlagSwatch" Property="Background" Value="#F1C40F"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding}" Value="Maritain">
                    <Setter TargetName="FlagSwatch" Property="Background" Value="#3498DB"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding}" Value="Norvos">
                    <Setter TargetName="FlagSwatch" Property="Background" Value="#E74C3C"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding}" Value="Valtro">
                    <Setter TargetName="FlagSwatch" Property="Background" Value="#000000"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding}" Value="Random">
                    <Setter TargetName="FlagSwatch" Property="Background" Value="Transparent"/>
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>

        <!-- Small header for each player slot -->
        <Style x:Key="PlayerHeader" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <!-- sys:int for comparison in this view -->
        <sys:Int32 x:Key="Players2">2</sys:Int32>
        <sys:Int32 x:Key="Players3">3</sys:Int32>
        <sys:Int32 x:Key="Players4">4</sys:Int32>
    </UserControl.Resources>

    <!-- Two columns: left (player count) | right (player information) -->
    <Grid Margin="12,4,12,4">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <!-- Left Column: Number of Players -->
        <StackPanel Grid.Column="0" Margin="8,0,8,0">
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Width="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale, Converter={StaticResource MultiplyConverter}, ConverterParameter=56}"
                        Height="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale, Converter={StaticResource MultiplyConverter}, ConverterParameter=56}"
                        Background="{DynamicResource AccentBrush}"
                        CornerRadius="28"
                        Margin="0,0,12,0">
                    <TextBlock Text="👥"
                               HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock.FontSize>
                            <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                     Converter="{StaticResource MultiplyConverter}" ConverterParameter="26"/>
                        </TextBlock.FontSize>
                    </TextBlock>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="PLAYERS" Style="{StaticResource SectionKicker}">
                        <TextBlock.FontSize>
                            <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                     Converter="{StaticResource MultiplyConverter}" ConverterParameter="16"/>
                        </TextBlock.FontSize>
                    </TextBlock>
                    <TextBlock Text="Choose number of participants" Style="{StaticResource SectionSub}">
                        <TextBlock.FontSize>
                            <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                     Converter="{StaticResource MultiplyConverter}" ConverterParameter="12"/>
                        </TextBlock.FontSize>
                    </TextBlock>
                </StackPanel>
            </Grid>

            <!-- Number-of-players selection -->
            <Border Style="{StaticResource CardBorderStyle}">
                <UniformGrid Rows="1" Columns="3">
                    <!-- 2 -->
                    <RadioButton GroupName="PlayerCount"
                                 Margin="0,0,8,0"
                                 IsChecked="{Binding ParentViewModel.NumberOfPlayers, Converter={StaticResource IntComparisonConverter}, ConverterParameter={StaticResource Players2}}">
                        <RadioButton.Template>
                            <ControlTemplate TargetType="RadioButton">
                                <Border x:Name="border"
                                        Background="{DynamicResource Surface3Brush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="2"
                                        CornerRadius="8"
                                        Cursor="Hand" Padding="16,0">
                                    <Border.Height>
                                        <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                                 Converter="{StaticResource MultiplyConverter}" ConverterParameter="50"/>
                                    </Border.Height>
                                    <Grid>
                                        <TextBlock Text="2"
                                                   Foreground="{DynamicResource ForegroundBrush}"
                                                   FontFamily="Consolas"
                                                   FontWeight="Bold"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center">
                                            <TextBlock.FontSize>
                                                <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                                         Converter="{StaticResource MultiplyConverter}" ConverterParameter="20"/>
                                            </TextBlock.FontSize>
                                        </TextBlock>
                                    </Grid>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                        <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </RadioButton.Template>
                    </RadioButton>

                    <!-- 3 -->
                    <RadioButton GroupName="PlayerCount"
                                 Margin="0,0,8,0"
                                 IsChecked="{Binding ParentViewModel.NumberOfPlayers, Converter={StaticResource IntComparisonConverter}, ConverterParameter={StaticResource Players3}}">
                        <RadioButton.Template>
                            <ControlTemplate TargetType="RadioButton">
                                <Border x:Name="border"
                                        Background="{DynamicResource Surface3Brush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="2"
                                        CornerRadius="8"
                                        Cursor="Hand" Padding="16,0">
                                    <Border.Height>
                                        <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                                 Converter="{StaticResource MultiplyConverter}" ConverterParameter="50"/>
                                    </Border.Height>
                                    <Grid>
                                        <TextBlock Text="3"
                                                   Foreground="{DynamicResource ForegroundBrush}"
                                                   FontFamily="Consolas"
                                                   FontWeight="Bold"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center">
                                            <TextBlock.FontSize>
                                                <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                                         Converter="{StaticResource MultiplyConverter}" ConverterParameter="20"/>
                                            </TextBlock.FontSize>
                                        </TextBlock>
                                    </Grid>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                        <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </RadioButton.Template>
                    </RadioButton>

                    <!-- 4 -->
                    <RadioButton GroupName="PlayerCount"
                                 IsChecked="{Binding ParentViewModel.NumberOfPlayers, Converter={StaticResource IntComparisonConverter}, ConverterParameter={StaticResource Players4}}">
                        <RadioButton.Template>
                            <ControlTemplate TargetType="RadioButton">
                                <Border x:Name="border"
                                        Background="{DynamicResource Surface3Brush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="2"
                                        CornerRadius="8"
                                        Cursor="Hand" Padding="16,0">
                                    <Border.Height>
                                        <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                                 Converter="{StaticResource MultiplyConverter}" ConverterParameter="50"/>
                                    </Border.Height>
                                    <Grid>
                                        <TextBlock Text="4"
                                                   Foreground="{DynamicResource ForegroundBrush}"
                                                   FontFamily="Consolas"
                                                   FontWeight="Bold"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center">
                                            <TextBlock.FontSize>
                                                <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                                         Converter="{StaticResource MultiplyConverter}" ConverterParameter="20"/>
                                            </TextBlock.FontSize>
                                        </TextBlock>
                                    </Grid>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                        <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </RadioButton.Template>
                    </RadioButton>
                </UniformGrid>
            </Border>
        </StackPanel>

        <!-- Divider -->
        <Border Grid.Column="1" Width="1" Margin="12,12" Opacity="0.22"
                Background="{DynamicResource BorderBrush}"/>

        <!-- Right Column: Player Information (countries + alliances) -->
        <StackPanel Grid.Column="2" Margin="8,0,8,0">
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Width="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale, Converter={StaticResource MultiplyConverter}, ConverterParameter=56}"
                        Height="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale, Converter={StaticResource MultiplyConverter}, ConverterParameter=56}"
                        Background="{DynamicResource AccentBrush}"
                        CornerRadius="28"
                        Margin="0,0,12,0">
                    <TextBlock Text="🏳️"
                               HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock.FontSize>
                            <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                     Converter="{StaticResource MultiplyConverter}" ConverterParameter="26"/>
                        </TextBlock.FontSize>
                    </TextBlock>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="PLAYER INFORMATION" Style="{StaticResource SectionKicker}">
                        <TextBlock.FontSize>
                            <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                     Converter="{StaticResource MultiplyConverter}" ConverterParameter="16"/>
                        </TextBlock.FontSize>
                    </TextBlock>
                    <TextBlock Text="Assign a country to each player and set alliances"
                               Style="{StaticResource SectionSub}">
                        <TextBlock.FontSize>
                            <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                     Converter="{StaticResource MultiplyConverter}" ConverterParameter="12"/>
                        </TextBlock.FontSize>
                    </TextBlock>
                </StackPanel>
            </Grid>

            <!-- Player info card -->
            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel>

                    <!-- Player 1 (no alliance toggle for Player 1) -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Player 1" Style="{StaticResource PlayerHeader}">
                            <TextBlock.FontSize>
                                <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                         Converter="{StaticResource MultiplyConverter}" ConverterParameter="13"/>
                            </TextBlock.FontSize>
                        </TextBlock>

                        <ComboBox Grid.Column="1"
                                  ItemsSource="{StaticResource CountryOptions}"
                                  SelectedItem="{Binding ParentViewModel.Player1Country, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  ItemTemplate="{StaticResource CountryItemTemplate}"
                                  FontFamily="Consolas"
                                  VerticalAlignment="Center">
                            <ComboBox.MinWidth>
                                <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                         Converter="{StaticResource MultiplyConverter}" ConverterParameter="200"/>
                            </ComboBox.MinWidth>
                            <ComboBox.Height>
                                <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                         Converter="{StaticResource MultiplyConverter}" ConverterParameter="34"/>
                            </ComboBox.Height>
                        </ComboBox>

                        <!-- Live flag swatch -->
                        <Border Grid.Column="2"
                                Width="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale, Converter={StaticResource MultiplyConverter}, ConverterParameter=24}"
                                Height="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale, Converter={StaticResource MultiplyConverter}, ConverterParameter=24}"
                                CornerRadius="3" BorderThickness="1"
                                BorderBrush="{DynamicResource BorderBrush}" Margin="10,0,0,0" VerticalAlignment="Center">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player1Country}" Value="Redonia">
                                            <Setter Property="Background" Value="#2ECC71"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player1Country}" Value="Sevaria">
                                            <Setter Property="Background" Value="#9E9E9E"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player1Country}" Value="Auria">
                                            <Setter Property="Background" Value="#F1C40F"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player1Country}" Value="Maritain">
                                            <Setter Property="Background" Value="#3498DB"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player1Country}" Value="Norvos">
                                            <Setter Property="Background" Value="#E74C3C"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player1Country}" Value="Valtro">
                                            <Setter Property="Background" Value="#000000"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>
                    </Grid>

                    <!-- Player 2 (includes alliance toggle) -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Player 2" Style="{StaticResource PlayerHeader}">
                            <TextBlock.FontSize>
                                <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                         Converter="{StaticResource MultiplyConverter}" ConverterParameter="13"/>
                            </TextBlock.FontSize>
                        </TextBlock>

                        <ComboBox Grid.Column="1"
                                  ItemsSource="{StaticResource CountryOptions}"
                                  SelectedItem="{Binding ParentViewModel.Player2Country, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  ItemTemplate="{StaticResource CountryItemTemplate}"
                                  FontFamily="Consolas"
                                  VerticalAlignment="Center">
                            <ComboBox.MinWidth>
                                <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                         Converter="{StaticResource MultiplyConverter}" ConverterParameter="200"/>
                            </ComboBox.MinWidth>
                            <ComboBox.Height>
                                <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                         Converter="{StaticResource MultiplyConverter}" ConverterParameter="34"/>
                            </ComboBox.Height>
                        </ComboBox>

                        <Border Grid.Column="2"
                                Width="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale, Converter={StaticResource MultiplyConverter}, ConverterParameter=24}"
                                Height="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale, Converter={StaticResource MultiplyConverter}, ConverterParameter=24}"
                                CornerRadius="3" BorderThickness="1"
                                BorderBrush="{DynamicResource BorderBrush}" Margin="10,0,10,0" VerticalAlignment="Center">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player2Country}" Value="Redonia">
                                            <Setter Property="Background" Value="#2ECC71"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player2Country}" Value="Sevaria">
                                            <Setter Property="Background" Value="#9E9E9E"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player2Country}" Value="Auria">
                                            <Setter Property="Background" Value="#F1C40F"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player2Country}" Value="Maritain">
                                            <Setter Property="Background" Value="#3498DB"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player2Country}" Value="Norvos">
                                            <Setter Property="Background" Value="#E74C3C"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player2Country}" Value="Valtro">
                                            <Setter Property="Background" Value="#000000"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>

                        <!-- Alliance toggle -->
                        <CheckBox Grid.Column="3"
                                  IsChecked="{Binding ParentViewModel.Player2IsAlly, Mode=TwoWay}"
                                  VerticalAlignment="Center"
                                  Margin="0,0,10,0">
                            <CheckBox.Template>
                                <ControlTemplate TargetType="CheckBox">
                                    <Border x:Name="ToggleBackground"
                                            Background="{DynamicResource Surface3Brush}"
                                            CornerRadius="15"
                                            Cursor="Hand">
                                        <Border.Width>
                                            <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                                     Converter="{StaticResource MultiplyConverter}" ConverterParameter="60"/>
                                        </Border.Width>
                                        <Border.Height>
                                            <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                                     Converter="{StaticResource MultiplyConverter}" ConverterParameter="30"/>
                                        </Border.Height>
                                        <Canvas>
                                            <Ellipse x:Name="ToggleCircle"
                                                     Fill="{DynamicResource ForegroundBrush}"
                                                     Canvas.Left="2"
                                                     Canvas.Top="2">
                                                <Ellipse.Width>
                                                    <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                                             Converter="{StaticResource MultiplyConverter}" ConverterParameter="26"/>
                                                </Ellipse.Width>
                                                <Ellipse.Height>
                                                    <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                                             Converter="{StaticResource MultiplyConverter}" ConverterParameter="26"/>
                                                </Ellipse.Height>
                                                <Ellipse.RenderTransform>
                                                    <TranslateTransform x:Name="ToggleTransform" X="0"/>
                                                </Ellipse.RenderTransform>
                                            </Ellipse>
                                        </Canvas>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="ToggleBackground" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                            <Trigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="ToggleTransform" Storyboard.TargetProperty="X" To="30" Duration="0:0:0.2"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.EnterActions>
                                            <Trigger.ExitActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="ToggleTransform" Storyboard.TargetProperty="X" To="0" Duration="0:0:0.2"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.ExitActions>
                                        </Trigger>
                                        <!-- When only 2 players, force enemy and disable -->
                                        <DataTrigger Binding="{Binding ParentViewModel.NumberOfPlayers}" Value="2">
                                            <Setter Property="IsEnabled" Value="False"/>
                                        </DataTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </CheckBox.Template>
                        </CheckBox>

                        <TextBlock Grid.Column="4"
                                   VerticalAlignment="Center"
                                   FontFamily="Consolas"
                                   FontWeight="Bold">
                            <TextBlock.FontSize>
                                <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                         Converter="{StaticResource MultiplyConverter}" ConverterParameter="12"/>
                            </TextBlock.FontSize>
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="Enemy"/>
                                    <Setter Property="Foreground" Value="{DynamicResource ForegroundSecondaryBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player2IsAlly}" Value="True">
                                            <Setter Property="Text" Value="Ally"/>
                                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ParentViewModel.NumberOfPlayers}" Value="2">
                                            <Setter Property="Text" Value="Enemy (locked for 2 players)"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>

                    <!-- Player 3 -->
                    <Grid Margin="0,0,0,12">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ParentViewModel.NumberOfPlayers}" Value="3">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding ParentViewModel.NumberOfPlayers}" Value="4">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Player 3" Style="{StaticResource PlayerHeader}">
                            <TextBlock.FontSize>
                                <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                         Converter="{StaticResource MultiplyConverter}" ConverterParameter="13"/>
                            </TextBlock.FontSize>
                        </TextBlock>

                        <ComboBox Grid.Column="1"
                                  ItemsSource="{StaticResource CountryOptions}"
                                  SelectedItem="{Binding ParentViewModel.Player3Country, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  ItemTemplate="{StaticResource CountryItemTemplate}"
                                  FontFamily="Consolas"
                                  VerticalAlignment="Center">
                            <ComboBox.MinWidth>
                                <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                         Converter="{StaticResource MultiplyConverter}" ConverterParameter="200"/>
                            </ComboBox.MinWidth>
                            <ComboBox.Height>
                                <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                         Converter="{StaticResource MultiplyConverter}" ConverterParameter="34"/>
                            </ComboBox.Height>
                        </ComboBox>

                        <Border Grid.Column="2"
                                Width="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale, Converter={StaticResource MultiplyConverter}, ConverterParameter=24}"
                                Height="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale, Converter={StaticResource MultiplyConverter}, ConverterParameter=24}"
                                CornerRadius="3" BorderThickness="1"
                                BorderBrush="{DynamicResource BorderBrush}" Margin="10,0,10,0" VerticalAlignment="Center">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player3Country}" Value="Redonia">
                                            <Setter Property="Background" Value="#2ECC71"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player3Country}" Value="Sevaria">
                                            <Setter Property="Background" Value="#9E9E9E"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player3Country}" Value="Auria">
                                            <Setter Property="Background" Value="#F1C40F"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player3Country}" Value="Maritain">
                                            <Setter Property="Background" Value="#3498DB"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player3Country}" Value="Norvos">
                                            <Setter Property="Background" Value="#E74C3C"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player3Country}" Value="Valtro">
                                            <Setter Property="Background" Value="#000000"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>

                        <!-- Alliance toggle -->
                        <CheckBox Grid.Column="3"
                                  IsChecked="{Binding ParentViewModel.Player3IsAlly, Mode=TwoWay}"
                                  VerticalAlignment="Center"
                                  Margin="0,0,10,0">
                            <CheckBox.Template>
                                <ControlTemplate TargetType="CheckBox">
                                    <Border x:Name="ToggleBackground"
                                            Background="{DynamicResource Surface3Brush}"
                                            CornerRadius="15"
                                            Cursor="Hand">
                                        <Border.Width>
                                            <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                                     Converter="{StaticResource MultiplyConverter}" ConverterParameter="60"/>
                                        </Border.Width>
                                        <Border.Height>
                                            <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                                     Converter="{StaticResource MultiplyConverter}" ConverterParameter="30"/>
                                        </Border.Height>
                                        <Canvas>
                                            <Ellipse x:Name="ToggleCircle"
                                                     Fill="{DynamicResource ForegroundBrush}"
                                                     Canvas.Left="2"
                                                     Canvas.Top="2">
                                                <Ellipse.Width>
                                                    <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                                             Converter="{StaticResource MultiplyConverter}" ConverterParameter="26"/>
                                                </Ellipse.Width>
                                                <Ellipse.Height>
                                                    <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                                             Converter="{StaticResource MultiplyConverter}" ConverterParameter="26"/>
                                                </Ellipse.Height>
                                                <Ellipse.RenderTransform>
                                                    <TranslateTransform x:Name="ToggleTransform" X="0"/>
                                                </Ellipse.RenderTransform>
                                            </Ellipse>
                                        </Canvas>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="ToggleBackground" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                            <Trigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="ToggleTransform" Storyboard.TargetProperty="X" To="30" Duration="0:0:0.2"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.EnterActions>
                                            <Trigger.ExitActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="ToggleTransform" Storyboard.TargetProperty="X" To="0" Duration="0:0:0.2"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.ExitActions>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </CheckBox.Template>
                        </CheckBox>

                        <TextBlock Grid.Column="4"
                                   VerticalAlignment="Center"
                                   FontFamily="Consolas"
                                   FontWeight="Bold">
                            <TextBlock.FontSize>
                                <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                         Converter="{StaticResource MultiplyConverter}" ConverterParameter="12"/>
                            </TextBlock.FontSize>
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="Enemy"/>
                                    <Setter Property="Foreground" Value="{DynamicResource ForegroundSecondaryBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player3IsAlly}" Value="True">
                                            <Setter Property="Text" Value="Ally"/>
                                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>

                    <!-- Player 4 -->
                    <Grid>
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ParentViewModel.NumberOfPlayers}" Value="4">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Player 4" Style="{StaticResource PlayerHeader}">
                            <TextBlock.FontSize>
                                <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                         Converter="{StaticResource MultiplyConverter}" ConverterParameter="13"/>
                            </TextBlock.FontSize>
                        </TextBlock>

                        <ComboBox Grid.Column="1"
                                  ItemsSource="{StaticResource CountryOptions}"
                                  SelectedItem="{Binding ParentViewModel.Player4Country, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  ItemTemplate="{StaticResource CountryItemTemplate}"
                                  FontFamily="Consolas"
                                  VerticalAlignment="Center">
                            <ComboBox.MinWidth>
                                <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                         Converter="{StaticResource MultiplyConverter}" ConverterParameter="200"/>
                            </ComboBox.MinWidth>
                            <ComboBox.Height>
                                <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                         Converter="{StaticResource MultiplyConverter}" ConverterParameter="34"/>
                            </ComboBox.Height>
                        </ComboBox>

                        <Border Grid.Column="2"
                                Width="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale, Converter={StaticResource MultiplyConverter}, ConverterParameter=24}"
                                Height="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale, Converter={StaticResource MultiplyConverter}, ConverterParameter=24}"
                                CornerRadius="3" BorderThickness="1"
                                BorderBrush="{DynamicResource BorderBrush}" Margin="10,0,10,0" VerticalAlignment="Center">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player4Country}" Value="Redonia">
                                            <Setter Property="Background" Value="#2ECC71"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player4Country}" Value="Sevaria">
                                            <Setter Property="Background" Value="#9E9E9E"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player4Country}" Value="Auria">
                                            <Setter Property="Background" Value="#F1C40F"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player4Country}" Value="Maritain">
                                            <Setter Property="Background" Value="#3498DB"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player4Country}" Value="Norvos">
                                            <Setter Property="Background" Value="#E74C3C"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player4Country}" Value="Valtro">
                                            <Setter Property="Background" Value="#000000"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>

                        <!-- Alliance toggle -->
                        <CheckBox Grid.Column="3"
                                  IsChecked="{Binding ParentViewModel.Player4IsAlly, Mode=TwoWay}"
                                  VerticalAlignment="Center"
                                  Margin="0,0,10,0">
                            <CheckBox.Template>
                                <ControlTemplate TargetType="CheckBox">
                                    <Border x:Name="ToggleBackground"
                                            Background="{DynamicResource Surface3Brush}"
                                            CornerRadius="15"
                                            Cursor="Hand">
                                        <Border.Width>
                                            <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                                     Converter="{StaticResource MultiplyConverter}" ConverterParameter="60"/>
                                        </Border.Width>
                                        <Border.Height>
                                            <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                                     Converter="{StaticResource MultiplyConverter}" ConverterParameter="30"/>
                                        </Border.Height>
                                        <Canvas>
                                            <Ellipse x:Name="ToggleCircle"
                                                     Fill="{DynamicResource ForegroundBrush}"
                                                     Canvas.Left="2"
                                                     Canvas.Top="2">
                                                <Ellipse.Width>
                                                    <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                                             Converter="{StaticResource MultiplyConverter}" ConverterParameter="26"/>
                                                </Ellipse.Width>
                                                <Ellipse.Height>
                                                    <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                                             Converter="{StaticResource MultiplyConverter}" ConverterParameter="26"/>
                                                </Ellipse.Height>
                                                <Ellipse.RenderTransform>
                                                    <TranslateTransform x:Name="ToggleTransform" X="0"/>
                                                </Ellipse.RenderTransform>
                                            </Ellipse>
                                        </Canvas>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="ToggleBackground" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                            <Trigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="ToggleTransform" Storyboard.TargetProperty="X" To="30" Duration="0:0:0.2"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.EnterActions>
                                            <Trigger.ExitActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="ToggleTransform" Storyboard.TargetProperty="X" To="0" Duration="0:0:0.2"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.ExitActions>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </CheckBox.Template>
                        </CheckBox>

                        <TextBlock Grid.Column="4"
                                   VerticalAlignment="Center"
                                   FontFamily="Consolas"
                                   FontWeight="Bold">
                            <TextBlock.FontSize>
                                <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                         Converter="{StaticResource MultiplyConverter}" ConverterParameter="12"/>
                            </TextBlock.FontSize>
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="Enemy"/>
                                    <Setter Property="Foreground" Value="{DynamicResource ForegroundSecondaryBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ParentViewModel.Player4IsAlly}" Value="True">
                                            <Setter Property="Text" Value="Ally"/>
                                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>

                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>