<UserControl x:Class="WWXMapEditor.Views.Configuration.FileLocationsStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WWXMapEditor.Views.Configuration"
             xmlns:scaling="clr-namespace:WWXMapEditor.UI.Scaling"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <StackPanel>
        <TextBlock Text="FILE LOCATIONS" 
                   Foreground="{DynamicResource ForegroundBrush}" 
                   FontFamily="Consolas"
                   FontSize="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale,
                                      Converter={StaticResource MultiplyConverter}, ConverterParameter=32}" 
                   FontWeight="Bold" 
                   Margin="0,0,0,30"/>
        
        <TextBlock Text="We'll create these folders if they don't exist" 
                   Foreground="{DynamicResource ForegroundSecondaryBrush}" 
                   FontFamily="Consolas"
                   FontSize="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale,
                                      Converter={StaticResource MultiplyConverter}, ConverterParameter=16}" 
                   Margin="0,0,0,20"/>
        
        <!-- Directory Settings -->
        <GroupBox Header="Default Directories" Style="{StaticResource SettingsGroupBoxStyle}">
            <StackPanel Margin="20">
                <Grid Margin="0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Text="Projects:" 
                               Foreground="{DynamicResource ForegroundBrush}" 
                               FontSize="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale,
                                                  Converter={StaticResource MultiplyConverter}, ConverterParameter=16}"
                               VerticalAlignment="Center"/>
                    
                    <TextBox Grid.Column="1" 
                             Text="{Binding DefaultProjectDirectory}" 
                             Style="{StaticResource SettingsTextBoxStyle}"
                             FontSize="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale,
                                                Converter={StaticResource MultiplyConverter}, ConverterParameter=14}"
                             Margin="0,0,10,0"/>
                    
                    <Button Grid.Column="2" 
                            Content="Browse..." 
                            Command="{Binding BrowseProjectDirectoryCommand}"
                            Style="{StaticResource EditorButtonStyle}"
                            Width="100"
                            Height="35"/>
                </Grid>
                
                <Grid Margin="0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Text="Tilesets:" 
                               Foreground="{DynamicResource ForegroundBrush}" 
                               FontSize="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale,
                                                  Converter={StaticResource MultiplyConverter}, ConverterParameter=16}"
                               VerticalAlignment="Center"/>
                    
                    <TextBox Grid.Column="1" 
                             Text="{Binding DefaultTilesetDirectory}" 
                             Style="{StaticResource SettingsTextBoxStyle}"
                             FontSize="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale,
                                                Converter={StaticResource MultiplyConverter}, ConverterParameter=14}"
                             Margin="0,0,10,0"/>
                    
                    <Button Grid.Column="2" 
                            Content="Browse..." 
                            Command="{Binding BrowseTilesetDirectoryCommand}"
                            Style="{StaticResource EditorButtonStyle}"
                            Width="100"
                            Height="35"/>
                </Grid>
                
                <Grid Margin="0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Text="Auto-save:" 
                               Foreground="{DynamicResource ForegroundBrush}" 
                               FontSize="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale,
                                                  Converter={StaticResource MultiplyConverter}, ConverterParameter=16}"
                               VerticalAlignment="Center"/>
                    
                    <TextBox Grid.Column="1" 
                             Text="{Binding AutoSaveLocation}" 
                             Style="{StaticResource SettingsTextBoxStyle}"
                             FontSize="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale,
                                                Converter={StaticResource MultiplyConverter}, ConverterParameter=14}"
                             Margin="0,0,10,0"/>
                    
                    <Button Grid.Column="2" 
                            Content="Browse..." 
                            Command="{Binding BrowseAutoSaveLocationCommand}"
                            Style="{StaticResource EditorButtonStyle}"
                            Width="100"
                            Height="35"/>
                </Grid>
            </StackPanel>
        </GroupBox>
    </StackPanel>
</UserControl>