<UserControl x:Class="WWXMapEditor.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WWXMapEditor.Views"
             xmlns:scaling="clr-namespace:WWXMapEditor.UI.Scaling"
             xmlns:conv="clr-namespace:WWXMapEditor.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1366"
             SnapsToDevicePixels="True"
             UseLayoutRounding="True"
             Loaded="UserControl_Loaded"
             MouseMove="UserControl_MouseMove">

    <!--
      AboutView — restyled to match the Main Menu/Configuration look
      - Parallax background, vignette, aurora drift, scanlines, dust motes.
      - Header with title and Close button (uses MenuButtonStyle).
      - Content displayed on a trapezoid "board" with overlays.
      - Keeps all original text, icon, features list, and bottom Close button.
    -->

    <UserControl.Resources>
        <conv:MultiplyConverter x:Key="MultiplyConverter"/>

        <!-- Aurora colors (same as main menu) -->
        <Color x:Key="Aurora1">#4400E58A</Color>
        <Color x:Key="Aurora2">#4420A4FF</Color>
        <Color x:Key="Aurora3">#44B770FF</Color>

        <!-- Header text styles -->
        <Style x:Key="HeaderTitleText" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>
        <Style x:Key="HeaderSubText" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundSecondaryBrush}"/>
            <Setter Property="Opacity" Value="0.9"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <!-- BACKGROUND LAYERS (parallax on background only) -->
        <Grid x:Name="BackgroundRoot">
            <Grid.RenderTransform>
                <TranslateTransform x:Name="BgParallax" X="0" Y="0"/>
            </Grid.RenderTransform>

            <!-- Fullscreen artwork -->
            <Image Source="pack://application:,,,/Resources/Images/MainMenu/Background.png"
                   Stretch="UniformToFill"
                   RenderOptions.BitmapScalingMode="HighQuality"/>

            <!-- Dark vignette -->
            <Rectangle>
                <Rectangle.Fill>
                    <RadialGradientBrush GradientOrigin="0.5,0.5" Center="0.5,0.5" RadiusX="0.95" RadiusY="0.95">
                        <GradientStop Color="#00000000" Offset="0.0"/>
                        <GradientStop Color="#D0000000" Offset="1.0"/>
                    </RadialGradientBrush>
                </Rectangle.Fill>
            </Rectangle>

            <!-- Gentle aurora overlay -->
            <Rectangle Opacity="0.30" IsHitTestVisible="False">
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop x:Name="Aur1" Color="{StaticResource Aurora1}" Offset="0"/>
                        <GradientStop x:Name="Aur2" Color="{StaticResource Aurora2}" Offset="0.5"/>
                        <GradientStop x:Name="Aur3" Color="{StaticResource Aurora3}" Offset="1"/>
                    </LinearGradientBrush>
                </Rectangle.Fill>
                <Rectangle.RenderTransform>
                    <TranslateTransform x:Name="AurMove" X="0" Y="0"/>
                </Rectangle.RenderTransform>
            </Rectangle>

            <!-- Subtle scanlines -->
            <Rectangle Opacity="0.05" IsHitTestVisible="False">
                <Rectangle.Fill>
                    <DrawingBrush TileMode="Tile" Viewport="0,0,3,3" ViewportUnits="Absolute">
                        <DrawingBrush.Drawing>
                            <DrawingGroup>
                                <GeometryDrawing Brush="#00000000" Geometry="M0,0 L3,0 L3,3 L0,3 Z"/>
                                <GeometryDrawing Brush="#20FFFFFF" Geometry="M0,0 L3,0 L3,1 L0,1 Z"/>
                            </DrawingGroup>
                        </DrawingBrush.Drawing>
                    </DrawingBrush>
                </Rectangle.Fill>
            </Rectangle>

            <!-- Dust motes -->
            <Canvas IsHitTestVisible="False" Opacity="0.18">
                <Ellipse Width="3" Height="3" Fill="#FFFFFFFF" Canvas.Left="180" Canvas.Top="260">
                    <Ellipse.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard RepeatBehavior="Forever">
                                    <DoubleAnimation Storyboard.TargetProperty="(Canvas.Left)" From="180" To="760" Duration="0:0:24" AutoReverse="True"/>
                                    <DoubleAnimation Storyboard.TargetProperty="(Canvas.Top)" From="260" To="140" Duration="0:0:24" AutoReverse="True"/>
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.1" To="0.5" Duration="0:0:6" AutoReverse="True"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Ellipse.Triggers>
                </Ellipse>
                <Ellipse Width="2" Height="2" Fill="#FFFFFFFF" Canvas.Left="900" Canvas.Top="620">
                    <Ellipse.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard RepeatBehavior="Forever">
                                    <DoubleAnimation Storyboard.TargetProperty="(Canvas.Left)" From="900" To="1180" Duration="0:0:22" AutoReverse="True"/>
                                    <DoubleAnimation Storyboard.TargetProperty="(Canvas.Top)" From="620" To="520" Duration="0:0:22" AutoReverse="True"/>
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.1" To="0.4" Duration="0:0:5" AutoReverse="True"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Ellipse.Triggers>
                </Ellipse>
            </Canvas>
        </Grid>

        <!-- FOREGROUND LAYOUT -->
        <DockPanel LastChildFill="True">
            <!-- Header with title and Close -->
            <Border DockPanel.Dock="Top"
                    Background="#00000000"
                    Padding="18,14,18,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Vertical" Grid.Column="0">
                        <TextBlock Text="ABOUT"
                                   Style="{StaticResource HeaderTitleText}">
                            <TextBlock.FontSize>
                                <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                         Converter="{StaticResource MultiplyConverter}" ConverterParameter="28"/>
                            </TextBlock.FontSize>
                            <TextBlock.Effect>
                                <DropShadowEffect Color="#AA000000" BlurRadius="10" ShadowDepth="2" Opacity="0.6"/>
                            </TextBlock.Effect>
                        </TextBlock>
                        <TextBlock Text="WWX Map Editor"
                                   Style="{StaticResource HeaderSubText}"
                                   Margin="2,4,0,0">
                            <TextBlock.FontSize>
                                <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                         Converter="{StaticResource MultiplyConverter}" ConverterParameter="14"/>
                            </TextBlock.FontSize>
                        </TextBlock>
                    </StackPanel>

                    <Button Grid.Column="1"
                            Content="✕"
                            Style="{StaticResource MenuButtonStyle}"
                            Width="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale, Converter={StaticResource MultiplyConverter}, ConverterParameter=56}"
                            Height="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale, Converter={StaticResource MultiplyConverter}, ConverterParameter=44}"
                            FontSize="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale, Converter={StaticResource MultiplyConverter}, ConverterParameter=22}"
                            Command="{Binding CloseCommand}"/>
                </Grid>
            </Border>

            <!-- Content on a trapezoid "board" -->
            <Grid Margin="18,0,18,18">
                <!-- Spacer below header so board never overlaps -->
                <Border Height="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale, Converter={StaticResource MultiplyConverter}, ConverterParameter=36}"
                        Background="#00000000"/>

                <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                    <!-- Board with trapezoid and overlays -->
                    <Grid x:Name="BoardContainer"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Center"
                          MinWidth="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale, Converter={StaticResource MultiplyConverter}, ConverterParameter=880}"
                          Height="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale, Converter={StaticResource MultiplyConverter}, ConverterParameter=620}">
                        <Grid.RenderTransform>
                            <TransformGroup>
                                <RotateTransform Angle="-6"/>
                                <TranslateTransform x:Name="BoardSlideTx" X="80" Y="0"/>
                            </TransformGroup>
                        </Grid.RenderTransform>

                        <Grid.CacheMode>
                            <BitmapCache/>
                        </Grid.CacheMode>

                        <!-- Trapezoid and overlays -->
                        <Viewbox Stretch="Fill" StretchDirection="Both">
                            <Grid Width="1180" Height="620">
                                <Path Stretch="Fill"
                                      Fill="#E00B0B0B"
                                      Stroke="#66FFFFFF"
                                      StrokeThickness="1">
                                    <Path.Data>
                                        <PathGeometry>
                                            <PathFigure StartPoint="80,0" IsClosed="True">
                                                <LineSegment Point="1180,0"/>
                                                <LineSegment Point="1110,620"/>
                                                <LineSegment Point="0,620"/>
                                            </PathFigure>
                                        </PathGeometry>
                                    </Path.Data>
                                    <Path.Effect>
                                        <DropShadowEffect BlurRadius="30" Direction="320" ShadowDepth="10" Color="#AA000000" Opacity="0.8"/>
                                    </Path.Effect>
                                </Path>

                                <!-- Right-side shadow -->
                                <Rectangle HorizontalAlignment="Right" Width="44" Height="620" IsHitTestVisible="False">
                                    <Rectangle.Fill>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#66000000" Offset="0.0"/>
                                            <GradientStop Color="#00000000" Offset="1.0"/>
                                        </LinearGradientBrush>
                                    </Rectangle.Fill>
                                </Rectangle>

                                <!-- Overlays -->
                                <Image Source="pack://application:,,,/Resources/Images/MainMenu/overlays/scratches.png"
                                       Stretch="UniformToFill"
                                       Opacity="0.10"
                                       IsHitTestVisible="False"
                                       RenderOptions.BitmapScalingMode="HighQuality"/>
                                <Image Source="pack://application:,,,/Resources/Images/MainMenu/overlays/splatters.png"
                                       Stretch="UniformToFill"
                                       Opacity="0.08"
                                       IsHitTestVisible="False"
                                       RenderOptions.BitmapScalingMode="HighQuality"/>
                            </Grid>
                        </Viewbox>

                        <!-- Board content -->
                        <Border Margin="56,46,64,46" Background="#00000000">
                            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Margin="0" HorizontalAlignment="Center" MaxWidth="820">
                                    <Image Source="pack://application:,,,/Resources/Images/Icons/app.ico" 
                                           Width="128" 
                                           Height="128" 
                                           Margin="0,0,0,20"/>

                                    <TextBlock Text="WWX MAP EDITOR" 
                                               FontFamily="Consolas"
                                               FontWeight="Bold"
                                               Foreground="{DynamicResource ForegroundBrush}"
                                               HorizontalAlignment="Center"
                                               Margin="0,0,0,10">
                                        <TextBlock.FontSize>
                                            <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                                     Converter="{StaticResource MultiplyConverter}" ConverterParameter="32"/>
                                        </TextBlock.FontSize>
                                    </TextBlock>

                                    <TextBlock Text="Version 1.0.0" 
                                               FontFamily="Consolas"
                                               Foreground="{DynamicResource ForegroundSecondaryBrush}"
                                               HorizontalAlignment="Center"
                                               Margin="0,0,0,30">
                                        <TextBlock.FontSize>
                                            <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                                     Converter="{StaticResource MultiplyConverter}" ConverterParameter="16"/>
                                        </TextBlock.FontSize>
                                    </TextBlock>

                                    <TextBlock TextWrapping="Wrap"
                                               FontFamily="Consolas"
                                               Foreground="{DynamicResource ForegroundBrush}"
                                               LineHeight="20"
                                               Margin="0,0,0,20">
                                        <TextBlock.FontSize>
                                            <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                                     Converter="{StaticResource MultiplyConverter}" ConverterParameter="14"/>
                                        </TextBlock.FontSize>
                                        <Run Text="WWX Map Editor is a powerful tool for creating and editing game maps. "/>
                                        <Run Text="Built with modern technology and designed for ease of use, "/>
                                        <Run Text="it provides all the features you need to bring your map ideas to life."/>
                                    </TextBlock>

                                    <TextBlock Text="Features:" 
                                               FontFamily="Consolas"
                                               FontWeight="Bold"
                                               Foreground="{DynamicResource ForegroundBrush}"
                                               Margin="0,0,0,10">
                                        <TextBlock.FontSize>
                                            <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                                     Converter="{StaticResource MultiplyConverter}" ConverterParameter="16"/>
                                        </TextBlock.FontSize>
                                    </TextBlock>

                                    <TextBlock TextWrapping="Wrap"
                                               FontFamily="Consolas"
                                               Foreground="{DynamicResource ForegroundBrush}"
                                               LineHeight="20"
                                               Margin="20,0,0,20">
                                        <TextBlock.FontSize>
                                            <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                                     Converter="{StaticResource MultiplyConverter}" ConverterParameter="14"/>
                                        </TextBlock.FontSize>
                                        <Run Text="• Intuitive map creation and editing"/>
                                        <LineBreak/>
                                        <Run Text="• Support for multiple terrain types"/>
                                        <LineBreak/>
                                        <Run Text="• Configurable victory conditions"/>
                                        <LineBreak/>
                                        <Run Text="• Fog of war settings"/>
                                        <LineBreak/>
                                        <Run Text="• Undo/Redo functionality"/>
                                        <LineBreak/>
                                        <Run Text="• And much more..."/>
                                    </TextBlock>

                                    <TextBlock Text="© 2025 WWX Map Editor Team" 
                                               FontFamily="Consolas"
                                               Foreground="{DynamicResource ForegroundSecondaryBrush}"
                                               HorizontalAlignment="Center"
                                               Margin="0,30,0,0">
                                        <TextBlock.FontSize>
                                            <Binding Source="{x:Static scaling:ScaleService.Instance}" Path="FontScale"
                                                     Converter="{StaticResource MultiplyConverter}" ConverterParameter="12"/>
                                        </TextBlock.FontSize>
                                    </TextBlock>
                                </StackPanel>
                            </ScrollViewer>
                        </Border>
                    </Grid>
                </Grid>
            </Grid>

            <!-- Bottom Bar (kept for parity with original) -->
            <Grid DockPanel.Dock="Bottom" Margin="18,0,18,18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Border Grid.ColumnSpan="2" Background="#00000000"/>
                <Button Grid.Column="1"
                        Content="CLOSE" 
                        Style="{StaticResource MenuButtonStyle}"
                        MinWidth="120"
                        Height="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale, Converter={StaticResource MultiplyConverter}, ConverterParameter=44}"
                        FontSize="{Binding Source={x:Static scaling:ScaleService.Instance}, Path=FontScale, Converter={StaticResource MultiplyConverter}, ConverterParameter=16}"
                        Command="{Binding CloseCommand}"/>
            </Grid>
        </DockPanel>

        <!-- Background motion animations -->
        <Grid.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard RepeatBehavior="Forever">
                        <!-- Aurora drift -->
                        <DoubleAnimation Storyboard.TargetName="AurMove"
                                         Storyboard.TargetProperty="X"
                                         From="-40" To="40"
                                         Duration="0:0:45"
                                         AutoReverse="True"/>
                        <DoubleAnimation Storyboard.TargetName="AurMove"
                                         Storyboard.TargetProperty="Y"
                                         From="18" To="-18"
                                         Duration="0:1:00"
                                         AutoReverse="True"/>

                        <!-- Slight midpoint shift -->
                        <DoubleAnimation Storyboard.TargetName="Aur2"
                                         Storyboard.TargetProperty="Offset"
                                         From="0.45" To="0.55"
                                         Duration="0:0:18"
                                         AutoReverse="True"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>

            <!-- Board entrance + mild slide-in -->
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetName="BoardSlideTx"
                                         Storyboard.TargetProperty="X"
                                         From="80" To="0"
                                         Duration="0:0:0.28">
                            <DoubleAnimation.EasingFunction>
                                <BackEase Amplitude="0.5" EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Grid.Triggers>
    </Grid>
</UserControl>