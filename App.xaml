<Application x:Class="WWXMapEditor.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
             xmlns:local="clr-namespace:WWXMapEditor"
             xmlns:converters="clr-namespace:WWXMapEditor.Converters"
             StartupUri="/Views/MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Theme Resources - Load theme first as base -->
                <ResourceDictionary Source="/Resources/Themes/DarkTheme.xaml"/>

                <!-- Main Styles Resource - Contains all styles -->
                <ResourceDictionary Source="/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Converters -->
            <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
            <converters:BooleanToInverseVisibilityConverter x:Key="BooleanToInverseVisibilityConverter"/>
            <converters:StringComparisonConverter x:Key="StringComparisonConverter"/>
            <converters:IntComparisonConverter x:Key="IntComparisonConverter"/>
            <converters:TerrainColorConverter x:Key="TerrainColorConverter"/>
            <converters:IndexToStepNumberConverter x:Key="IndexToStepNumberConverter"/>
            <converters:BooleanToIndexConverter x:Key="BooleanToIndexConverter"/>

            <!-- Built-in WPF Converter -->
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

            <!-- Global Values -->
            <System:Double x:Key="MinWindowWidth">1280</System:Double>
            <System:Double x:Key="MinWindowHeight">720</System:Double>
            <System:Double x:Key="DefaultWindowWidth">1920</System:Double>
            <System:Double x:Key="DefaultWindowHeight">1080</System:Double>

            <!-- Animation Durations -->
            <Duration x:Key="FastAnimationDuration">0:0:0.2</Duration>
            <Duration x:Key="MediumAnimationDuration">0:0:0.3</Duration>
            <Duration x:Key="SlowAnimationDuration">0:0:0.5</Duration>

            <!-- Easing Functions -->
            <QuadraticEase x:Key="StandardEase" EasingMode="EaseInOut"/>
            <CubicEase x:Key="EntranceEase" EasingMode="EaseOut"/>
            <CubicEase x:Key="ExitEase" EasingMode="EaseIn"/>

            <!-- Global Error Template -->
            <ControlTemplate x:Key="ValidationErrorTemplate">
                <DockPanel>
                    <Border BorderBrush="Red" BorderThickness="1">
                        <AdornedElementPlaceholder/>
                    </Border>
                    <TextBlock Foreground="Red" 
                               FontSize="12" 
                               Text="!" 
                               FontWeight="Bold" 
                               Margin="5,0,0,0"/>
                </DockPanel>
            </ControlTemplate>

            <!-- Global ToolTip Style -->
            <Style TargetType="ToolTip">
                <Setter Property="Background" Value="{DynamicResource Surface2Brush}"/>
                <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="8,6"/>
                <Setter Property="FontFamily" Value="Consolas"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToolTip">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Global ScrollBar Style -->
            <Style TargetType="ScrollBar">
                <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="Width" Value="12"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ScrollBar">
                            <Grid Background="{TemplateBinding Background}">
                                <Track Name="PART_Track" IsDirectionReversed="True">
                                    <Track.Thumb>
                                        <Thumb>
                                            <Thumb.Template>
                                                <ControlTemplate TargetType="Thumb">
                                                    <Border Background="{DynamicResource Surface3Brush}"
                                                            CornerRadius="2"
                                                            Margin="2"/>
                                                </ControlTemplate>
                                            </Thumb.Template>
                                        </Thumb>
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ScrollViewer Style -->
            <Style TargetType="ScrollViewer">
                <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
                <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ScrollViewer">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <ScrollContentPresenter Grid.Column="0" Grid.Row="0"/>

                                <ScrollBar x:Name="PART_VerticalScrollBar"
                                           Grid.Column="1" Grid.Row="0"
                                           Orientation="Vertical"
                                           Value="{TemplateBinding VerticalOffset}"
                                           Maximum="{TemplateBinding ScrollableHeight}"
                                           ViewportSize="{TemplateBinding ViewportHeight}"
                                           Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>

                                <ScrollBar x:Name="PART_HorizontalScrollBar"
                                           Grid.Column="0" Grid.Row="1"
                                           Orientation="Horizontal"
                                           Value="{TemplateBinding HorizontalOffset}"
                                           Maximum="{TemplateBinding ScrollableWidth}"
                                           ViewportSize="{TemplateBinding ViewportWidth}"
                                           Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>