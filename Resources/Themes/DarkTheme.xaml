<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Core colors -->
    <Color x:Key="BackgroundColor">#FF0A0A0F</Color>
    <!-- very dark blue-black -->
    <Color x:Key="SurfaceColor">#FF121418</Color>
    <!-- base panel -->
    <Color x:Key="Surface2Color">#FF181B20</Color>
    <!-- secondary panel -->
    <Color x:Key="Surface3Color">#E0101010</Color>
    <!-- deep slab (matches slats) -->
    <Color x:Key="BorderColor">#FF2F3947</Color>
    <!-- general app border -->
    <Color x:Key="ForegroundColor">#FFF3F7FA</Color>
    <!-- primary text -->
    <Color x:Key="ForegroundSecondaryColor">#FFB8C0CC</Color>

    <!-- Accent tuned to Main Menu pale green -->
    <Color x:Key="AccentColor">#FFCFE3B0</Color>

    <!-- Hover/Pressed tints similar to main menu treatment -->
    <Color x:Key="HoverColor">#1AFFFFFF</Color>
    <!-- ~10% white overlay -->
    <Color x:Key="PressedColor">#33111111</Color>
    <!-- ~20% dark press -->

    <!-- Light/divider lines used in menu slats and board -->
    <Color x:Key="LightBorderColor">#66FFFFFF</Color>
    <!-- 40% white -->
    <Color x:Key="DividerStrongColor">#33FFFFFF</Color>
    <!-- 20% white -->
    <Color x:Key="DividerSoftColor">#22FFFFFF</Color>
    <!-- 13% white -->

    <!-- Shadows -->
    <Color x:Key="ShadowColor">#AA000000</Color>

    <!-- Status colors (used for gradients etc.) -->
    <Color x:Key="SuccessStartColor">#FF4CAF50</Color>
    <Color x:Key="SuccessEndColor">#FF45A049</Color>
    <Color x:Key="DangerStartColor">#FFFF5252</Color>
    <Color x:Key="DangerEndColor">#FFFF1744</Color>

    <!-- Aurora (parallax overlay) aligned with MainMenuView) -->
    <Color x:Key="NeonShadowColor">#FF00E58A</Color>
    <Color x:Key="Aurora1">#4400E58A</Color>
    <Color x:Key="Aurora2">#4420A4FF</Color>
    <Color x:Key="Aurora3">#44B770FF</Color>

    <!-- Brushes from colors -->
    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
    <SolidColorBrush x:Key="Surface2Brush" Color="{StaticResource Surface2Color}"/>
    <SolidColorBrush x:Key="Surface3Brush" Color="{StaticResource Surface3Color}"/>
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>

    <SolidColorBrush x:Key="ForegroundBrush" Color="{StaticResource ForegroundColor}"/>
    <SolidColorBrush x:Key="ForegroundSecondaryBrush" Color="{StaticResource ForegroundSecondaryColor}"/>

    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
    <SolidColorBrush x:Key="HoverBrush" Color="{StaticResource HoverColor}"/>
    <SolidColorBrush x:Key="PressedBrush" Color="{StaticResource PressedColor}"/>

    <!-- Extra brushes used by the main menu visuals -->
    <SolidColorBrush x:Key="LightBorderBrush" Color="{StaticResource LightBorderColor}"/>
    <SolidColorBrush x:Key="DividerStrongBrush" Color="{StaticResource DividerStrongColor}"/>
    <SolidColorBrush x:Key="DividerSoftBrush" Color="{StaticResource DividerSoftColor}"/>

    <!-- Success/Danger gradients -->
    <LinearGradientBrush x:Key="SuccessGradientBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="{StaticResource SuccessStartColor}" Offset="0"/>
        <GradientStop Color="{StaticResource SuccessEndColor}" Offset="1"/>
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="DangerGradientBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="{StaticResource DangerStartColor}" Offset="0"/>
        <GradientStop Color="{StaticResource DangerEndColor}" Offset="1"/>
    </LinearGradientBrush>

    <!-- Pale-green selection fill (used by MainMenuView; duplicated here for consistency) -->
    <SolidColorBrush x:Key="MenuHighlightBrush" Color="#CFE3B0" Opacity="0.28"/>

    <!-- Aurora brushes (optional if colors are bound directly) -->
    <SolidColorBrush x:Key="Aurora1Brush" Color="{StaticResource Aurora1}"/>
    <SolidColorBrush x:Key="Aurora2Brush" Color="{StaticResource Aurora2}"/>
    <SolidColorBrush x:Key="Aurora3Brush" Color="{StaticResource Aurora3}"/>

    <!-- Subtle neon gradient if needed for accents elsewhere -->
    <LinearGradientBrush x:Key="NeonBorderBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#FF00E58A" Offset="0"/>
        <GradientStop Color="#FF20A4FF" Offset="1"/>
    </LinearGradientBrush>

    <!-- Image assets used by the main menu (slat mask + tiled selection texture)
         Ensure these exist:
         - Resources/Images/MainMenu/overlays/slat_mask.png
         - Resources/Images/MainMenu/overlays/selection_texture.png
         Build Action = Resource
    -->
    <ImageBrush x:Key="SlatOpacityMaskBrush"
                ImageSource="pack://application:,,,/Resources/Images/MainMenu/overlays/slat_mask.png"
                Stretch="Fill"/>

    <!-- Tile small noise so it stays crisp at any size -->
    <ImageBrush x:Key="SelectionTextureBrush"
                ImageSource="pack://application:,,,/Resources/Images/MainMenu/overlays/selection_texture.png"
                TileMode="Tile"
                ViewportUnits="Absolute"
                Viewport="0,0,24,24"
                Stretch="Fill"/>

    <!-- Specialized brushes for view content that must be theme-aware -->
    <!-- Terrain swatches -->
    <SolidColorBrush x:Key="TerrainPlainsBrush" Color="#90EE90"/>
    <SolidColorBrush x:Key="TerrainMountainBrush" Color="#8B8989"/>
    <SolidColorBrush x:Key="TerrainForestBrush" Color="#228B22"/>
    <SolidColorBrush x:Key="TerrainSandBrush" Color="#E2C27B"/>
    <SolidColorBrush x:Key="TerrainSeaBrush" Color="#2F6CB3"/>

    <!-- Country flag colors -->
    <SolidColorBrush x:Key="CountryRedoniaBrush" Color="#2ECC71"/>
    <SolidColorBrush x:Key="CountrySevariaBrush" Color="#9E9E9E"/>
    <SolidColorBrush x:Key="CountryAuriaBrush" Color="#F1C40F"/>
    <SolidColorBrush x:Key="CountryMaritainBrush" Color="#3498DB"/>
    <SolidColorBrush x:Key="CountryNorvosBrush" Color="#E74C3C"/>
    <SolidColorBrush x:Key="CountryValtroBrush" Color="#000000"/>

    <!-- Fog/shroud preview brushes -->
    <SolidColorBrush x:Key="ShroudBlackBrush" Color="#FF000000"/>
    <SolidColorBrush x:Key="ShroudGreyBrush" Color="#FF808080"/>

    <!-- Base button style that views can BasedOn (kept minimal to avoid breaking other screens) -->
    <Style x:Key="MenuButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
        <Setter Property="Background" Value="{DynamicResource Surface3Brush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource LightBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource Surface2Brush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{DynamicResource PressedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.6"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>